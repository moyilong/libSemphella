#include "libERT.h"

LIBERT_API CryptoStatus Crypto(const char * password, const char * orig_data, char * dest, const uint64_t real_len, const char algorthim)
{
	CryptoPlus head;
	head.account_level = level;
	head.algorthim = algorthim;

	return CPLUS_OK;
}
